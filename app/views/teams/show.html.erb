<main class="<%=@team.id %>">
<h1><%= @team.name %></h1>
  <article>
<%= image_tag @team.image_url %>
  </article>
</main>
<footer>
  <%= link_to "[Edit]", edit_team_path(@team.id) %>
  <%= link_to "[Delete]", team_path(@team.id), method: :delete, data: { confirm: "Are you sure?" } %>
</footer>
